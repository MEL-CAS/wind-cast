# --- frontend/.env.local ---
# URL of the deployed backend API (Render). For local dev: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# --- backend (Render environment variables) ---
# Origin allowed by CORS — the deployed frontend URL (Vercel). For local dev: http://localhost:3000
FRONTEND_ORIGIN=http://localhost:3000
